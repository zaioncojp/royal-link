<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>近未来ダッシュボード - ROYAL LINK</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    :root {
      --primary-color: #12c2e9;
      --secondary-color: #c471ed;
      --accent-color: #f64f59;
      --bg-color: #1b1b2f;
      --card-bg: rgba(255, 255, 255, 0.1);
      --text-color: #ffffff;
      --text-secondary: #c0c0c0;
      --border-radius: 16px;
      --box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
      --neon-glow: 0 0 8px rgba(18, 194, 233, 0.8), 0 0 16px rgba(196, 113, 237, 0.8), 0 0 24px rgba(246, 79, 89, 0.8);
    }

    body {
      font-family: 'Orbitron', sans-serif;
      background-color: var(--bg-color);
      margin: 0;
      padding: 0;
      color: var(--text-color);
    }

    .dashboard-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .dashboard-header {
      text-align: center;
      margin-bottom: 30px;
    }

    .dashboard-header h1 {
      font-size: 2.5rem;
      margin: 0;
      color: var(--primary-color);
      text-shadow: var(--neon-glow);
    }

    .dashboard-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      width: 100%;
      margin-bottom: 30px;
    }

    .stat-card {
      background: var(--card-bg);
      border-radius: var(--border-radius);
      box-shadow: var(--box-shadow);
      padding: 20px;
      text-align: center;
      backdrop-filter: blur(10px);
      transition: transform 0.3s, box-shadow 0.3s;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .stat-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--neon-glow);
    }

    .stat-value {
      font-size: 2.5rem;
      color: var(--primary-color);
      margin: 10px 0;
    }

    .stat-title {
      font-size: 1.2rem;
      color: var(--text-secondary);
    }

    .url-shortener {
      background: var(--card-bg);
      border-radius: var(--border-radius);
      box-shadow: var(--box-shadow);
      padding: 30px;
      width: 100%;
      max-width: 600px;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .url-shortener h2 {
      font-size: 1.8rem;
      color: var(--primary-color);
      margin-bottom: 20px;
      text-shadow: var(--neon-glow);
    }

    .shorten-form {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    .shorten-form input {
      padding: 15px;
      border: none;
      border-radius: var(--border-radius);
      font-size: 1rem;
      background: rgba(255, 255, 255, 0.1);
      color: var(--text-color);
      backdrop-filter: blur(5px);
      box-shadow: inset 0 0 8px rgba(0, 0, 0, 0.5);
    }

    .shorten-btn {
      background: linear-gradient(to right, var(--primary-color), var(--secondary-color), var(--accent-color));
      color: #fff;
      border: none;
      padding: 15px;
      border-radius: var(--border-radius);
      font-size: 1.2rem;
      cursor: pointer;
      transition: background 0.3s, transform 0.2s;
      text-shadow: var(--neon-glow);
    }

    .shorten-btn:hover {
      transform: translateY(-3px);
    }
  </style>
</head>
<body>
  <div class="dashboard-container">
    <!-- ヘッダー -->
    <header class="dashboard-header">
      <h1>近未来ダッシュボード</h1>
    </header>

    <!-- 統計カード -->
    <section class="dashboard-stats">
      <div class="stat-card">
        <h2 class="stat-value">120</h2>
        <p class="stat-title">短縮URL数</p>
      </div>
      <div class="stat-card">
        <h2 class="stat-value">5400</h2>
        <p class="stat-title">クリック数</p>
      </div>
      <div class="stat-card">
        <h2 class="stat-value">75%</h2>
        <p class="stat-title">コンバージョン率</p>
      </div>
    </section>

    <!-- URL短縮フォーム -->
    <section class="url-shortener">
      <h2>URLを短縮する</h2>
      <form class="shorten-form" action="/shorten" method="POST">
        <input type="url" name="originalUrl" placeholder="例: https://example.com" required>
        <button type="submit" class="shorten-btn">短縮する</button>
      </form>
    </section>
  </div>
</body>
</html>