<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ログイン - ROYAL LINK</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <style>
    :root {
      --primary-color: #6e8efb;
      --primary-light: #a777e3;
      --primary-gradient: linear-gradient(135deg, #6e8efb, #a777e3);
      --secondary-color: #2d3748;
      --dark-color: #1a202c;
      --light-color: #f7fafc;
      --error-color: #f56565;
      --success-color: #48bb78;
      --glass-bg: rgba(255, 255, 255, 0.15);
      --glass-border: rgba(255, 255, 255, 0.2);
      --glass-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      --neon-glow: 0 0 10px rgba(110, 142, 251, 0.6), 0 0 20px rgba(110, 142, 251, 0.4);
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    body {
      background: #0f0c29;
      background: linear-gradient(to right, #24243e, #302b63, #0f0c29);
      color: white;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      height: 100vh;
      margin: 0;
      overflow: hidden;
    }
    
    .auth-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      padding: 20px;
      position: relative;
    }
    
    .auth-container::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: radial-gradient(circle at center, transparent 30%, rgba(0, 0, 0, 0.7) 100%);
      z-index: -1;
    }
    
    .auth-card {
      background: var(--glass-bg);
      backdrop-filter: blur(16px);
      border: 1px solid var(--glass-border);
      border-radius: 16px;
      box-shadow: var(--glass-shadow);
      padding: 40px;
      width: 100%;
      max-width: 450px;
      overflow: hidden;
      position: relative;
      z-index: 1;
    }
    
    .auth-card::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle at center, rgba(110, 142, 251, 0.2) 0%, transparent 50%);
      z-index: -1;
      animation: pulse 20s infinite;
    }
    
    @keyframes pulse {
      0% { transform: translate(0, 0); }
      25% { transform: translate(5%, 5%); }
      50% { transform: translate(0, 10%); }
      75% { transform: translate(-5%, 5%); }
      100% { transform: translate(0, 0); }
    }
    
    .auth-card h1 {
      text-align: center;
      margin-bottom: 30px;
      color: white;
      font-weight: 300;
      letter-spacing: 2px;
      font-size: 2rem;
      text-shadow: 0 0 10px rgba(110, 142, 251, 0.6);
    }
    
    .form-group {
      margin-bottom: 25px;
      position: relative;
    }
    
    .form-group label {
      display: block;
      margin-bottom: 10px;
      font-weight: 300;
      letter-spacing: 1px;
      font-size: 0.9rem;
      transition: all 0.3s;
      opacity: 0.8;
    }
    
    .form-group input {
      width: 100%;
      padding: 15px 20px;
      background: rgba(255, 255, 255, 0.07);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 8px;
      color: white;
      font-size: 1rem;
      transition: all 0.3s;
      box-sizing: border-box;
    }
    
    .form-group input:focus {
      outline: none;
      border-color: rgba(110, 142, 251, 0.6);
      box-shadow: 0 0 15px rgba(110, 142, 251, 0.4);
    }
    
    .btn-primary {
      width: 100%;
      padding: 15px;
      border: none;
      border-radius: 8px;
      background: var(--primary-gradient);
      color: white;
      font-size: 1rem;
      font-weight: 500;
      letter-spacing: 1px;
      cursor: pointer;
      transition: all 0.3s;
      text-transform: uppercase;
      margin-top: 10px;
      box-shadow: var(--neon-glow);
    }
    
    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 0 20px rgba(110, 142, 251, 0.8), 0 0 40px rgba(110, 142, 251, 0.6);
    }
    
    .btn-primary:active {
      transform: translateY(1px);
    }
    
    .auth-links {
      text-align: center;
      margin-top: 25px;
      font-size: 0.9rem;
      opacity: 0.8;
    }
    
    .auth-links a {
      color: #a777e3;
      text-decoration: none;
      transition: all 0.3s;
    }
    
    .auth-links a:hover {
      color: #6e8efb;
      text-shadow: 0 0 5px rgba(110, 142, 251, 0.8);
    }
    
    .alert {
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 25px;
      display: flex;
      align-items: center;
      background: rgba(231, 76, 60, 0.1);
      border-left: 3px solid #e74c3c;
    }
    
    .alert i {
      margin-right: 10px;
      font-size: 1.2rem;
    }
    
    .glow-effect {
      position: absolute;
      width: 200px;
      height: 200px;
      border-radius: 50%;
      background: rgba(110, 142, 251, 0.1);
      filter: blur(60px);
    }
    
    .glow-1 {
      top: -100px;
      left: -100px;
    }
    
    .glow-2 {
      bottom: -100px;
      right: -100px;
      background: rgba(167, 119, 227, 0.1);
    }
    
    .floating-icon {
      position: absolute;
      opacity: 0.1;
      z-index: -1;
    }
    
    .icon-1 {
      top: 20%;
      left: 10%;
      font-size: 2rem;
      animation: float1 15s infinite;
    }
    
    .icon-2 {
      bottom: 20%;
      right: 10%;
      font-size: 1.5rem;
      animation: float2 20s infinite;
    }
    
    @keyframes float1 {
      0%, 100% { transform: translate(0, 0) rotate(0deg); }
      25% { transform: translate(20px, -30px) rotate(90deg); }
      50% { transform: translate(0, -50px) rotate(180deg); }
      75% { transform: translate(-20px, -20px) rotate(270deg); }
    }
    
    @keyframes float2 {
      0%, 100% { transform: translate(0, 0) rotate(0deg); }
      25% { transform: translate(-20px, 30px) rotate(-90deg); }
      50% { transform: translate(0, 50px) rotate(-180deg); }
      75% { transform: translate(20px, 20px) rotate(-270deg); }
    }
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="glow-effect glow-1"></div>
    <div class="glow-effect glow-2"></div>
    
    <i class="fas fa-link floating-icon icon-1"></i>
    <i class="fas fa-globe floating-icon icon-2"></i>
    
    <div class="auth-card">
      <h1>ROYAL LINK</h1>
      
      <% if (error) { %>
        <div class="alert">
          <i class="fas fa-exclamation-circle"></i> <%= error %>
        </div>
      <% } %>
      
      <form action="/login" method="POST">
        <div class="form-group">
          <label for="username">ユーザー名 または メールアドレス</label>
          <input type="text" id="username" name="username" placeholder="ユーザー名またはメールアドレス" required>
        </div>
        
        <div class="form-group">
          <label for="password">パスワード</label>
          <input type="password" id="password" name="password" placeholder="••••••••" required>
        </div>
        
        <button type="submit" class="btn-primary">ログイン</button>
      </form>
      
      <div class="auth-links">
        <p>アカウントをお持ちでない方は <a href="/register">新規登録</a></p>
        <p><a href="/">ホームに戻る</a></p>
      </div>
    </div>
  </div>
</body>
</html>