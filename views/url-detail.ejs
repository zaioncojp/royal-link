<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL詳細 - ROYAL LINK</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
  <style>
    :root {
      --primary-color: #6e8efb;
      --primary-light: #a777e3;
      --primary-gradient: linear-gradient(135deg, #6e8efb, #a777e3);
      --secondary-color: #2d3748;
      --dark-color: #1a202c;
      --light-color: #f7fafc;
      --error-color: #f56565;
      --success-color: #48bb78;
      --text-color: #e2e8f0;
      --text-muted: #a0aec0;
      --glass-bg: rgba(26, 32, 44, 0.6);
      --glass-border: rgba(255, 255, 255, 0.1);
      --chart-bg: rgba(26, 32, 44, 0.4);
    }
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    
    body {
      background: linear-gradient(to right, #24243e, #302b63, #0f0c29);
      color: var(--text-color);
      min-height: 100vh;
      line-height: 1.6;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem;
    }
    
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }
    
    .header h1 {
      font-size: 2rem;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }
    
    .back-link {
      display: inline-flex;
      align-items: center;
      color: var(--text-color);
      text-decoration: none;
      padding: 0.5rem 1rem;
      border-radius: 8px;
      background: rgba(255, 255, 255, 0.1);
      transition: all 0.3s;
    }
    
    .back-link:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: translateY(-2px);
    }
    
    .back-link i {
      margin-right: 0.5rem;
    }
    
    .alert {
      padding: 1rem;
      border-radius: 8px;
      margin-bottom: 1.5rem;
      display: flex;
      align-items: center;
    }
    
    .alert i {
      margin-right: 0.75rem;
      font-size: 1.2rem;
    }
    
    .alert-error {
      background: rgba(245, 101, 101, 0.2);
      border-left: 4px solid var(--error-color);
    }
    
    .alert-success {
      background: rgba(72, 187, 120, 0.2);
      border-left: 4px solid var(--success-color);
    }
    
    .card {
      background: var(--glass-bg);
      backdrop-filter: blur(10px);
      border-radius: 12px;
      overflow: hidden;
      border: 1px solid var(--glass-border);
      box-shadow: 0 8px 32px rgba(31, 38, 135, 0.3);
      margin-bottom: 2rem;
    }
    
    .card-header {
      padding: 1.5rem;
      background: rgba(26, 32, 44, 0.8);
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 1px solid var(--glass-border);
    }
    
    .card-header h2 {
      margin: 0;
      font-size: 1.5rem;
      display: flex;
      align-items: center;
    }
    
    .card-header h2 i {
      margin-right: 0.75rem;
      color: var(--primary-light);
    }
    
    .card-body {
      padding: 1.5rem;
    }
    
    .detail-info {
      background: var(--chart-bg);
      border-radius: 8px;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
    }
    
    .detail-group {
      display: flex;
      margin-bottom: 1rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid var(--glass-border);
    }
    
    .detail-group:last-child {
      margin-bottom: 0;
      padding-bottom: 0;
      border-bottom: none;
    }
    
    .detail-label {
      width: 150px;
      font-weight: 600;
      color: var(--text-muted);
    }
    
    .detail-value {
      flex: 1;
    }
    
    .copy-btn {
      background: none;
      border: none;
      color: var(--primary-light);
      cursor: pointer;
      font-size: 0.9rem;
      padding: 0.25rem 0.5rem;
      margin-left: 0.5rem;
      border-radius: 4px;
      transition: all 0.3s;
    }
    
    .copy-btn:hover {
      background: rgba(110, 142, 251, 0.2);
    }
    
    .chart-container {
      height: 300px;
      display: flex;
      align-items: flex-end;
      justify-content: space-around;
      margin: 1.5rem 0;
    }
    
    .chart-bar {
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 30px;
      margin: 0 2px;
    }
    
    .bar {
      width: 100%;
      background: var(--primary-gradient);
      border-radius: 4px 4px 0 0;
      transition: height 0.5s ease;
      position: relative;
    }
    
    .bar-label {
      font-size: 0.7rem;
      margin-top: 0.5rem;
      color: var(--text-muted);
    }
    
    .bar-value {
      position: absolute;
      top: -20px;
      left: 50%;
      transform: translateX(-50%);
      font-size: 0.7rem;
      color: var(--text-color);
      background: rgba(26, 32, 44, 0.8);
      padding: 0.2rem 0.4rem;
      border-radius: 4px;
      white-space: nowrap;
    }
    
    .chart-description {
      text-align: center;
      margin-top: 1rem;
      padding: 1rem;
      background: var(--chart-bg);
      border-radius: 8px;
      font-size: 0.9rem;
    }
    
    .table-responsive {
      overflow-x: auto;
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
    }
    
    table th,
    table td {
      padding: 0.75rem 1rem;
      text-align: left;
      border-bottom: 1px solid var(--glass-border);
    }
    
    table th {
      font-weight: 600;
      color: var(--text-muted);
      background: rgba(26, 32, 44, 0.6);
    }
    
    table tr:hover {
      background: rgba(255, 255, 255, 0.05);
    }
    
    .empty-message {
      text-align: center;
      padding: 2rem;
      color: var(--text-muted);
    }
    
    .log-summary {
      text-align: center;
      margin-top: 1rem;
      font-size: 0.9rem;
      color: var(--text-muted);
    }
    
    @media (max-width: 768px) {
      .container {
        padding: 1rem;
      }
      
      .header {
        flex-direction: column;
        align-items: flex-start;
      }
      
      .back-link {
        margin-top: 1rem;
      }
      
      .detail-group {
        flex-direction: column;
      }
      
      .detail-label {
        width: 100%;
        margin-bottom: 0.5rem;
      }
      
      table {
        min-width: 600px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header class="header">
      <h1>URL詳細</h1>
      <a href="/dashboard" class="back-link">
        <i class="fas fa-arrow-left"></i> ダッシュボードに戻る
      </a>
    </header>
    
    <% if (error) { %>
      <div class="alert alert-error">
        <i class="fas fa-exclamation-circle"></i> <%= error %>
      </div>
    <% } %>
    
    <% if (success) { %>
      <div class="alert alert-success">
        <i class="fas fa-check-circle"></i> <%= success %>
      </div>
    <% } %>
    
    <!-- URL Basic Information -->
    <div class="card">
      <div class="card-header">
        <h2><i class="fas fa-info-circle"></i> 基本情報</h2>
      </div>
      <div class="card-body">
        <div class="detail-info">
          <div class="detail-group">
            <div class="detail-label">元のURL</div>
            <div class="detail-value">
              <a href="<%= url.originalUrl %>" target="_blank"><%= url.originalUrl %></a>
            </div>
          </div>
          
          <div class="detail-group">
            <div class="detail-label">短縮URL</div>
            <div class="detail-value">
              <% const shortUrl = `https://${appDomain}/s/${url.shortCode}`; %>
              <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
              <button class="copy-btn" data-url="<%= shortUrl %>">
                <i class="fas fa-copy"></i> コピー
              </button>
            </div>
          </div>
          
          <div class="detail-group">
            <div class="detail-label">クリック数</div>
            <div class="detail-value"><%= url.clicks || 0 %>回</div>
          </div>
          
          <div class="detail-group">
            <div class="detail-label">ショートコード</div>
            <div class="detail-value"><%= url.shortCode %></div>
          </div>
          
          <div class="detail-group">
            <div class="detail-label">作成日時</div>
            <div class="detail-value"><%= new Date(url.createdAt).toLocaleString() %></div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Time-based Analytics -->
    <div class="card">
      <div class="card-header">
        <h2><i class="fas fa-chart-bar"></i> 時間帯別アクセス</h2>
      </div>
      <div class="card-body">
        <div class="chart-container">
          <% for (let i = 0; i < hourlyData.length; i++) { %>
            <div class="chart-bar">
              <div class="bar" style="height: <%= Math.max(5, (hourlyData[i].count / Math.max(...hourlyData.map(h => h.count || 1), 1) * 250)) %>px">
                <% if (hourlyData[i].count > 0) { %>
                  <div class="bar-value"><%= hourlyData[i].count %></div>
                <% } %>
              </div>
              <div class="bar-label"><%= hourlyData[i].hour %>時</div>
            </div>
          <% } %>
        </div>
        
        <div class="chart-description">
          <% 
            let maxHour = 0;
            let maxCount = 0;
            
            for (let i = 0; i < hourlyData.length; i++) {
              if (hourlyData[i].count > maxCount) {
                maxCount = hourlyData[i].count;
                maxHour = hourlyData[i].hour;
              }
            }
          %>
          
          <% if (maxCount > 0) { %>
            <p>最もアクセスが多い時間帯は <strong><%= maxHour %>:00 - <%= (maxHour + 1) % 24 %>:00</strong> で、全体の <%= Math.round((maxCount / (url.clicks || 1)) * 100) %>% のアクセスがこの時間帯に集中しています。</p>
          <% } else { %>
            <p>まだアクセスデータがありません。短縮URLがクリックされると、時間帯別のアクセス統計が表示されます。</p>
          <% } %>
        </div>
      </div>
    </div>
    
    <!-- Daily Analytics -->
    <% if (dailyChartData && dailyChartData.length > 0) { %>
      <div class="card">
        <div class="card-header">
          <h2><i class="fas fa-calendar-alt"></i> 日別アクセス</h2>
        </div>
        <div class="card-body">
          <div class="chart-container">
            <% for (let i = 0; i < dailyChartData.length; i++) { %>
              <div class="chart-bar">
                <div class="bar" style="height: <%= Math.max(5, (dailyChartData[i].count / Math.max(...dailyChartData.map(d => d.count), 1) * 250)) %>px">
                  <div class="bar-value"><%= dailyChartData[i].count %></div>
                </div>
                <div class="bar-label">
                  <%= new Date(dailyChartData[i].date).toLocaleDateString('ja-JP', { month: 'numeric', day: 'numeric' }) %>
                </div>
              </div>
            <% } %>
          </div>
          
          <div class="chart-description">
            <p>日別のアクセス状況を表示しています。直近 <%= dailyChartData.length %> 日間のデータが含まれています。</p>
          </div>
        </div>
      </div>
    <% } %>
    
    <!-- Access Logs -->
    <div class="card">
      <div class="card-header">
        <h2><i class="fas fa-list"></i> アクセスログ</h2>
      </div>
      <div class="card-body">
        <% if (url.accessLogs && url.accessLogs.length > 0) { %>
          <div class="table-responsive">
            <table>
              <thead>
                <tr>
                  <th>日時</th>
                  <th>IPアドレス</th>
                  <th>ブラウザ情報</th>
                  <th>リファラー</th>
                </tr>
              </thead>
              <tbody>
                <% 
                  // 最新の10件を表示
                  const recentLogs = [...url.accessLogs]
                    .sort((a, b) => new Date(b.timestamp) - new Date(a.timestamp))
                    .slice(0, 10);
                %>
                
                <% recentLogs.forEach(log => { %>
                  <tr>
                    <td><%= new Date(log.timestamp).toLocaleString() %></td>
                    <td><%= log.ipAddress %></td>
                    <td>
                      <% if (log.userAgent && log.userAgent.length > 30) { %>
                        <%= log.userAgent.substring(0, 30) %>...
                      <% } else { %>
                        <%= log.userAgent || '不明' %>
                      <% } %>
                    </td>
                    <td><%= log.referer || '直接アクセス' %></td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
          
          <div class="log-summary">
            <p>全 <%= url.accessLogs.length %> 件のアクセスログのうち最新の10件を表示しています。</p>
          </div>
        <% } else { %>
          <div class="empty-message">
            <i class="fas fa-info-circle"></i>
            <p>まだアクセスログがありません。短縮URLがクリックされると、ここにアクセス情報が表示されます。</p>
          </div>
        <% } %>
      </div>
    </div>
  </div>
  
  <script>
    // Copy button functionality
    document.addEventListener('DOMContentLoaded', function() {
      const copyButtons = document.querySelectorAll('.copy-btn');
      
      copyButtons.forEach(button => {
        button.addEventListener('click', function() {
          const url = this.getAttribute('data-url');
          navigator.clipboard.writeText(url)
            .then(() => {
              // Show success indicator
              const originalIcon = this.innerHTML;
              this.innerHTML = '<i class="fas fa-check"></i> コピー完了';
              setTimeout(() => {
                this.innerHTML = originalIcon;
              }, 2000);
            })
            .catch(err => {
              console.error('コピーに失敗しました:', err);
              alert('URLのコピーに失敗しました');
            });
        });
      });
    });
  </script>
</body>
</html>